---
import BaseLayout from '../layouts/BaseLayout.astro';
import avatar from '../avatar.png';
---

<BaseLayout title="Zoe's Blog" description="刘周莹的个人主页">
  <div class="page">
    <!-- Hero -->
    <div class="hero">
      <div class="hero-info">
        <h1>刘周莹</h1>
        <p class="hero-tags">
          <span>女 · 21岁</span>
          <span>安徽理工大学</span>
          <span>数字媒体技术</span>
        </p>
        <p class="hero-goal">AI产品运营</p>
      </div>
      <img src={avatar.src} alt="avatar" class="hero-img" />
    </div>

    <!-- Bio -->
    <div class="bio">
      <p>
        大学期间，我一直在做一件事——<strong>搞清楚 AI 产品怎么从想法走到用户手中</strong>。我主导过 AI 智能交互产品从需求调研到用户增长验证的全流程，拿过全国一等奖，也踩过不少坑。
      </p>
      <p>
        我相信产品的灵魂不在代码里，在用户的使用场景里。所以我很喜欢跟用户聊，每次深访都能发现一些"原来你们是这么想的"的时刻。
      </p>
    </div>

    <!-- Projects -->
    <div class="section">
      <h2 class="section-title">竞赛与项目</h2>

      <div class="project-card">
        <div class="project-badge gold">全国一等奖</div>
        <h3>全国大学生智能技术应用大赛</h3>
        <p class="project-role">项目负责人 · AI 智能交互产品</p>
        <p class="project-desc">全国 200 余所高校参赛，经校赛、区域评审、全国总决赛三轮选拔，获当届最高奖项。统筹 5 人跨专业团队，独立完成 20 余位目标用户深访、6 款竞品拆解、产品场景定义与功能需求文档。路演环节搭建现场体验机制让评委直接上手试用，赛后整理 200 余条用户反馈并逐项落地迭代。</p>
      </div>

      <div class="project-card">
        <div class="project-badge silver">国家级二等奖</div>
        <h3>中国大学生工程实践与创新能力大赛</h3>
        <p class="project-role">项目负责人 · 智能数字化科普产品</p>
        <p class="project-desc">教育部 A 类学科竞赛，年参赛逾十万人次。经三版方案迭代确立"入门趣味互动 + 进阶深度内容"分层策略。线下引导 200 余位用户试用，收集 300 余条有效反馈，独立完成数据清洗与问题归因，输出产品改进清单。</p>
      </div>

      <div class="project-card">
        <div class="project-badge bronze">省级重点</div>
        <h3>大学生创新创业训练计划</h3>
        <p class="project-role">独立申报与执行</p>
        <p class="project-desc">独立完成市场调研、用户画像、商业模式论证与全套申报材料撰写，通过省级专家评审获批重点立项。组织 5 场校园宣讲覆盖 500 余人次，形成面向非技术用户的 AI 产品宣讲方法论。</p>
      </div>
    </div>

    <!-- Skills -->
    <div class="section">
      <h2 class="section-title">技能</h2>
      <div class="skills">
        <div class="skill-item">AI 工具 · ChatGPT / Claude / Cursor</div>
        <div class="skill-item">数据分析 · Excel</div>
        <div class="skill-item">方案撰写与汇报</div>
        <div class="skill-item">用户研究与深访</div>
        <div class="skill-item">视觉设计 · 素描六级</div>
        <div class="skill-item">CET-4</div>
      </div>
    </div>

    <!-- Club -->
    <div class="section">
      <h2 class="section-title">社团</h2>
      <div class="project-card">
        <div class="project-badge gold">五星级会长</div>
        <h3>格调平面设计协会</h3>
        <p class="project-desc">接手时四星级社团，一年内搭建标准化管理与人才培养体系，完成星级升级评审答辩升至五星级（校内最高评定）。独立策划执行 10 余场线下活动，全年覆盖 10000+ 人次，零流程事故。管理 20 余人干事团队，多场活动并行有序推进。</p>
      </div>
    </div>

    <!-- Links -->
    <div class="contact">
      <a href="mailto:1516212410@qq.com" class="contact-link">Email</a>
      <a href="/about" class="contact-link">博客</a>
      <span class="contact-loc">安徽 · 淮南</span>
    </div>
  </div>
</BaseLayout>

<style>
  .page {
    max-width: 600px;
    margin: 0 auto;
  }

  /* Hero */
  .hero {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1.5rem;
    margin-bottom: 2.5rem;
  }

  .hero-info h1 {
    font-size: 2.2rem;
    font-weight: 600;
    margin-bottom: 0.6rem;
    background: linear-gradient(135deg, var(--accent), var(--text-link));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    line-height: 1.2;
  }

  .hero-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    margin-bottom: 0.3rem;
  }

  .hero-tags span {
    color: var(--text-muted);
    font-size: 0.9rem;
    background: var(--accent-soft);
    padding: 0.2rem 0.7rem;
    border-radius: 12px;
  }

  .hero-goal {
    color: var(--text-link);
    font-size: 0.9rem;
    font-weight: 500;
    margin-top: 0.3rem;
  }

  .hero-img {
    width: 96px;
    height: 96px;
    border-radius: var(--radius);
    border: 2px solid var(--accent);
    object-fit: cover;
    flex-shrink: 0;
    box-shadow: 0 4px 16px rgba(220, 140, 160, 0.18);
  }

  /* Bio */
  .bio {
    background: var(--bg-card);
    border-left: 3px solid var(--accent);
    border-radius: 0 var(--radius) var(--radius) 0;
    padding: 1.2rem 1.5rem;
    margin-bottom: 2.5rem;
    line-height: 1.9;
    font-size: 0.95rem;
  }

  .bio p {
    margin: 0.5rem 0;
  }

  .bio p:first-child {
    margin-top: 0;
  }

  .bio p:last-child {
    margin-bottom: 0;
  }

  /* Sections */
  .section {
    margin-bottom: 2.5rem;
  }

  .section-title {
    font-size: 1.15rem;
    font-weight: 600;
    color: var(--text);
    margin-bottom: 1rem;
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  .section-title::before {
    content: '';
    width: 4px;
    height: 18px;
    background: linear-gradient(180deg, var(--accent), var(--text-link));
    border-radius: 2px;
  }

  /* Project cards */
  .project-card {
    background: var(--bg-card);
    border: 1.5px solid var(--border);
    border-radius: var(--radius);
    padding: 1.3rem 1.5rem;
    margin-bottom: 0.8rem;
  }

  .project-badge {
    display: inline-block;
    font-size: 0.72rem;
    font-weight: 600;
    padding: 0.15rem 0.65rem;
    border-radius: 8px;
    margin-bottom: 0.5rem;
  }

  .project-badge.gold {
    color: #b45309;
    background: #fef3c7;
    border: 1px solid #fde68a;
  }
  .project-badge.silver {
    color: #6b7280;
    background: #f3f4f6;
    border: 1px solid #e5e7eb;
  }
  .project-badge.bronze {
    color: #92400e;
    background: #fef3c7;
    border: 1px solid #fde68a;
  }

  :root.dark .project-badge.gold {
    color: #fbbf24;
    background: #451a03;
    border-color: #78350f;
  }
  :root.dark .project-badge.silver {
    color: #d1d5db;
    background: #1f2937;
    border-color: #374151;
  }
  :root.dark .project-badge.bronze {
    color: #fbbf24;
    background: #451a03;
    border-color: #78350f;
  }

  .project-card h3 {
    font-size: 1rem;
    font-weight: 600;
    color: var(--text);
    margin-bottom: 0.2rem;
  }

  .project-role {
    font-size: 0.85rem;
    color: var(--text-muted);
    margin-bottom: 0.6rem;
  }

  .project-desc {
    font-size: 0.9rem;
    line-height: 1.8;
    color: var(--text);
  }

  /* Skills */
  .skills {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
  }

  .skill-item {
    font-size: 0.85rem;
    color: var(--text);
    background: var(--bg-card);
    border: 1.5px solid var(--border);
    padding: 0.4rem 0.9rem;
    border-radius: 20px;
  }

  /* Contact */
  .contact {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 1rem;
    padding-top: 1.5rem;
    border-top: 1.5px solid var(--border);
    font-size: 0.9rem;
  }

  .contact-link {
    color: var(--text-link);
    text-decoration: none;
    font-weight: 500;
  }

  .contact-link:hover {
    text-decoration: underline;
  }

  .contact-loc {
    color: var(--text-muted);
  }

  @media (max-width: 480px) {
    .hero-img {
      width: 80px;
      height: 80px;
    }
    .hero-info h1 {
      font-size: 1.7rem;
    }
  }
</style>
